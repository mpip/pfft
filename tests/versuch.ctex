\del§#include <complex.h>§
#include <fftw3-mpi.h>
\del§#include <pfft.h>§

\del§
static fftw_complex my_function(
    ptrdiff_t i, ptrdiff_t k, ptrdiff_t j
    )
{
  return i +1.0/(j+1) + I*1.0/(k+1);
}
§

Der versuch klappt \del§nicht§.

1.) Hier kommt ein kniffliger Fall:
2.) [In dieser Zeile sollte §nichts§ folgen:] \del§ Aber hier steht
3.) noch etwas.§ [Das sollte am Zeilenanfang stehen.]
4.) Zeile 2. und 3. müssen erhalten bleiben  

int main(int argc, char **argv)
{
\red§
    const ptrdiff_t n0 = 4, n1 = 4, n2 = 4;
    fftw_plan plan;
    fftw_complex *data;
§
    ptrdiff_t \red§alloc_local,§ local_n0, local_0_start, i, j, k;

    \red§MPI_Init(&argc, &argv);§
    fftw_mpi_init();

